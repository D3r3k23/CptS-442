#include "geometry.h"
#include "rectangle.h"


const Point3 Rectangle::operator()(const double u, const double v,
                            Vector3 &tangentU, Vector3 &tangentV) const
{
    //
    // ASSIGNMENT (PA10)
    //
    // This function will map (u, v) to the 3D position on the square
    // whose lower left (0, 0) corner is `pLL`, whose u-edge is `vU`
    // and whose v-edge is `vV`.
    //
    // 3 lines in instructor solution (YMMV)
    //
    return Point3(); // replace (permits template to compile cleanly)
}
