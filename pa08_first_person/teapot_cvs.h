#include "geometry.h"
extern
// These are the Bezier control vertices for Martin Newell's famous
// teapot (affinely transformed for our model), so treat them with
// respect!

const Point3 teapotBezierPatches[32][4][4];

const int nTeapotBezierPatches = 32;

